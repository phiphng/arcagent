{
  "name": "arcagent-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Jest unit tests for arcagent Telegram bot",
  "scripts": {
    "test": "jest --verbose",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
